# cursor-agent-demo

Minimal Node.js + Express todo app with a single page for adding, filtering, completing, and removing todos. The UI is dark mode only.
Todos are persisted locally with SQLite.

## Run

```bash
npm install
npm start
```

Then open [http://localhost:3000](http://localhost:3000).

The database file is created automatically at `data/todos.db`.